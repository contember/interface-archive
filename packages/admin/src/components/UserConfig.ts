import { defineUserConfig } from '@contember/react-utils'

export const contemberDefaults = {
	'StyleProvider.className.lucide-icons-override': false,
}

export const UserConfig = defineUserConfig(contemberDefaults)
